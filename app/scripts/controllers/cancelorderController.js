// This is a file copied by your subgenerator
define([
    './controllers'

    , './cancelorder/listController'

], function (mod) {
    mod.controller('cancelorderController', controllerFn);

    controllerFn.$injector = ['$scope', '$injector'];
    function controllerFn($scope, $injector) {

    };
});
