// This is a file copied by your subgenerator
/**
 * 默认的产品首页和默认的跳转规则
 */
define([
      '../../states'
      , '../../../cons/simpleCons'
      , './paymethods',//app 支付方式 配置
    ],
    function(stateModule, cons) {
    });
